A SharePoint folder is the only connector that will allow the import of multiple Excel (or CSV) files stored in a OneDrive for Business folder, without using a data gateway. 
Transposing treats rows as columns, and columns as rows. it wouldn't create new single column for data like unpivot.
The pivot column operation converts data into a table by aggregating values in a column. for example you can pivot the columns to calculate the count of product subcategories in each product category. 

The Power BI service provides support for creating Power BI dashboards. The Power BI mobile app can be used to view dashboards, but not to create them.
Power BI Desktop does not provide support
for creating Power BI dashboards.
The Pin visual option allows you to pin the visual to an existing dashboard or create a new one. 
Workspace apps can be installed automatically for any user in an organization, and without requiring notifications of installation.
You can add members to a workspace, but they receive a notification that they’ve been added to the workspace.
USERPRINCIPALNAME would return the Microsoft Entra ID login in the format of “name@company.com”
Contributor role is appropriate for users who need to manage content, as it provides permissions for creating, editing, and deleting content
assigning the Member role to all users provides more permissions than necessary for those who only need viewing access.
The Admin role is the only one that has the permission to update workspace metadata.
Workspace apps can be installed automatically for any user in an organization, and without requiring notifications of installation
The Contributor role is the least privileged role that grants permissions to schedule data refreshes.
Admin role is the only role to update metadata.
Admin users who have permissions to certify a semantic model are defined in the Dataset Certification tenant admin setting.


If there are 4 consecutive semantic model refresh failures, the refresh schedule will be automatically disabled.
To use row-level security to ensure that, once the report is published, employees in each department can only see the sales data for that department,
you first need to create a role
and a corresponding DAX expression.
Defining RLS roles in Power BI Desktop allows for filtering data based on department membership, making it an essential step for implementing RLS.
To restrict data access based on department membership, assigning users to roles in the Power BI service ensures that the Row-Level Security (RLS) rules 
are applied to the correct individuals.

https://learn.microsoft.com/en-us/power-bi/natural-language/end-user-q-and-a-tutorial


VISUALS
--------
To create a Power BI visual that displays trending data over months, you can use a ribbon chart or an area chart

A ribbon chart places the highest (ranked) value at the top of the stacked column each month and shows those ranked changes over time


A scatterplot is typically used to compare a relationship between two (or more) calculations and their categorical distribution between each other.

Date/time and numeric data types support bins-based grouping. Binary, Boolean, and text data types do not support bins-based grouping.

A SharePoint folder is the only connector that will allow the import of multiple Excel (or CSV) files stored in a OneDrive for Business folder, without using 
a data gateway.
A parameter is the only part of a query that can be updated or changed in the Power BI service, by accessing the dataset settings. 
The top and bottom entries in Value distribution identify outliers, which appear, respectively, the greatest and the smallest number of times in that column.
Enabling Assume Referential Integrity improves query efficiency by allowing INNER JOINs instead of OUTER JOINs, optimizing DirectQuery performance.
The ability to combine data from multiple semantic models is one of the features that is available in dashboards but not supported in reports.
Individual filters and slicers cannot be pinned to a dashboard.
certifying a semantic model is part of the endorsement process, which helps organizations highlight trusted, high-quality content for reuse across reports and 
dashboards.
Admin users who have permissions to certify a semantic model are defined in the Dataset Certification tenant admin setting.
there are 4 consecutive semantic model refresh failures, the refresh schedule will be automatically disabled.
The Admin role is the only one that has the permission to update workspace metadata.
A calculated table only duplicates data. Any model configurations such as column visibility or hierarchies must be recreated if needed.
Unlike a measure, a calculated column can be used in a slicer to place filter options on the report page. DAX measures cannot be placed in the “Filters on this
page” well.
They can only be placed per visual, in the “Filters on this visual” well of the Filters Pane. Both DAX columns and measures may be used as a visual-level filter. 
Both DAX columns and measures can be used in the drillthrough well.
Time intelligence functions have performance implications and are disabled for quick measures against DirectQuery tables. Mathematical operations, aggregate per category, and X-functions 
are all supported against DirectQuery.
Viewing,Editing and deleting needs contributer role.
Two benefits of using DirectQuery instead of Import are minimized local disk space usage and minimized need for data refresh
Two benefits of using Import instead of DirectQuery are full support for the Q&A Power BI service and full support for the Quick Insights Power BI service
Both the DirectQuery and Import storage modes support per table configuration
Quick Insights in Power BI Service is a feature that automatically analyzes your dataset to uncover hidden patterns, trends, and anomalies—without requiring you to
write any queries or build visuals manually.

The slicer visualization can be used to filter the other visuals on the page. You can enable a search box in a slicer where users can search for values 
to filter.
A funnel visualization is a chart that that has sequential connected stages, where items flow sequentially from one stage to the next. 
A matrix visualization displays data in two or more dimensions and cross-highlights with other visuals on the same report page.
A scatter chart visualization is a chart with one set of numerical data along a horizontal axis and another set of numerical values along a vertical axis.
The matrix, scatter chart, and funnel do not offer a search box.
A calculated table only duplicates data. Any model configurations such as column visibility or hierarchies must be recreated if needed.
Calculated tables are generated with DAX queries. Numeric range parameters create a table and measure, both generated with DAX queries.
Time intelligence functions have performance implications and are disabled for quick measures against DirectQuery tables

As part of setting up row-level security, it is necessary to create a role. In addition, it is necessary to add a DAX expression filter to the Salesperson table.
This expression checks for a match between the salesperson's User Principal Name attribute and the value in the UPN column. If the two match, the salesperson is
allowed to view the content of the corresponding row from the Salesperson table.
A column that has a lot of repeated values and a low number of distinct values will have a low cardinality. Reducing the number of distinct values reduces cardinality. 

To eliminate the impact of visual caching during a performance evaluation when using Performance Analyzer to evaluate the visuals, you must first add a blank page to the report,
select that page, and then reopen the PBIX file. Connecting DAX Studio to the data model is the first step to clear the data engine cache.

RLS roles are created or modified from the Manage roles space. You can assign AD users/groups to an existing role in the security settings,
but new RLS roles must be created in Power BI desktop or other model authoring external tool. RLS configurations cannot be accessed from any Power Query window.

Date/time and numeric data types support bins-based grouping. Binary, Boolean, and text data types do not support bins-based grouping.

The group is created against a numerical column using bins.The Bin group type is an auto grouping of items into bucketed bins (groups). It will display well as a histogram. 

To define incremental refresh policy
-------------------------------------
>Define the filter parameters.
>Use the parameters to apply a filter.
>Define the incremental refresh policy.
>Publish changes to Power BI service.

For incremental refresh, the semantic models are filtered by two date/time parameters: RangeStart and RangeEnd. 
Incremental refresh policy in defined in Power BI Desktop.
Once policy is defined, then need to publish the report to Power BI Service.
Incremental refresh is supported for Power BI Premium, Premium per user, Power BI Pro, and Power BI Embedded models.


Power BI provides three ways to endorse your semantic models:
-------------------------------------------------------------
Promotion - Enables you to highlight content that you think is valuable, worthwhile, and ready for others to use. It encourages the collaborative spread of content within the organization.
Certification - Request certification for a promoted semantic model. Certification can be a highly selective process, so only the truly reliable and authoritative semantic models are used across the organization.
Master data - Being labeled as master data means that the data item is regarded by the organization as being core, single-source-of-truth data, such as customer lists or product codes.
This is a feature from Microsoft Fabric, which we won't further discuss in this module.

Lineage View
-------------
Lineage View in Power BI is a powerful visual tool that helps you understand how data flows through your workspace — from its original source all the way to dashboards and reports.
Every workspace automatically has a lineage view. You need at least a Contributor role in the workspace to view it.


Q.You need to implement a solution that meets the following requirements:
The navigation destination must change based on the output of a DAX measure named [Error Rate].
If [Error Rate] is greater than 5%, the button must display the text “Error Rate” and navigate to the Error Rate page.
Otherwise, the button must display the text “On-time Rate” and navigate to the On-Time Rate page.
What three actions should you perform? 

Answer:
1.Create a DAX measure that outputs the correct page name based on the value of [Error Rate].
2.Then configure the button to use page navigation and use the newly created DAX measure to specify the navigation destination.
3.To change the button text to match the page name, conditional formatting must be used to set the text to equal the newly created DAX measure.


Manage and Secure Power BI


Configure relationships:
--------------------------------------------------------------------------
Each relationship has a single "from" column and a single "to" column. It's important that the data types for these columns are the same (or equivalent) and that they contain matching values.
If data source has a multi-column key,then need to transform the data to produce single-column keys for the related tables.
For One-to-one cardinality,best practice is to use Power Query to merge queries to produce a single model table.
Cross-filter direction determines how filters propagate.
There can only be one active filter propagation path between two model tables. 
The Synonyms property allows you to set one or more alternative names for the table
The Auto date/time feature in Power BI automatically creates hidden date tables for each date column in your data model.
Time Intelligence Generates 
>.A calculated table using the CALENDAR function for each date column
>.Adds columns like Year, Quarter, Month, Day to support drill-downs and filtering
>.Creates a hierarchy (Year → Quarter → Month → Day) for visuals
>.Automatically links the hidden date table to your original date column
The relationship between fact and dimension tables is typically a one-to-many relationship, where one record in a dimension table can relate to multiple records in a 
fact table.
The "one" side is always a dimension table while the "many" side is always a fact table.
A measure is a fact table column that stores values to be summarized,like SUM, MIN, MAX, AVERAGE.
ALLSELECTED('Table') respects the slicers and filters in report page.
USERELATIONSHIP function in DAX is used to activate an inactive relationship between two tables temporarily, specifically for the duration of a calculation.
Delivery Sales = 
CALCULATE(
    [Total Sales],
    USERELATIONSHIP(Sales[Delivery Date], Date[Date])
)
Nested DEX functions upto 64 levels can be used in calculated columns.

CLEAN , TRANSFORM, LOAD DATA IN POWER BI
-----------------------------------------
Data type of column can be changed from two places. Powe Query Editor and Power BI Desktop Report view.
Best practice is to change datatype in Power Query Editor before loading the data.
Changing column datatype is called Changed Type and will iterated every time data refreshed.
Two different ways of combining tables are merging and appending.

SHAPE AND COMBINE DATA IN POWER BI 
----------------------------------
https://learn.microsoft.com/en-us/power-bi/connect-data/desktop-shape-and-combine-data


Websites for Sample Data:
---------------------------
Kaggle: A platform with a vast collection of datasets across various domains. You can search for datasets specifically suited for data analysis and visualization.
Microsoft Sample Data: Microsoft provides sample datasets specifically designed for Power BI. You can find them in the Power BI documentation or directly in the Power BI Desktop under the "Get Data" option.
Data.gov: A repository of U.S. government data that offers a wide range of datasets for public use.
World Bank Data: Access global development data, including economic, social, and environmental indicators.
Google Dataset Search: A tool that helps you find datasets stored across the web, making it easier to locate data relevant to your interests.

Some of the Datasets for Practice:
----------------------------------
https://www.kaggle.com/datasets/elemento/nyc-yellow-taxi-trip-data?ref=hackernoon.com
https://www.kaggle.com/datasets/theworldbank/world-development-indicators?ref=hackernoon.com
https://www.kaggle.com/datasets/shaikhshoeb/adventureworks-dataset-for-data-analysis
https://www.kaggle.com/datasets/samolkin/adventure-works-sales/code
https://powerbidocs.com/tag/sample-superstore-sales-excel-xls/?ref=hackernoon.com
https://learn.microsoft.com/en-us/power-bi/create-reports/sample-datasets#the-power-bi-samples-as-pbix-files
********************************************************** BI LABS 
https://github.com/MicrosoftLearning/PL-300-Microsoft-Power-BI-Data-Analyst/tree/Main/Allfiles
https://github.com/MicrosoftLearning/PL-300-Microsoft-Power-BI-Data-Analyst/blob/Main/Instructions/Labs/01-get-data-in-power-bi.md
********************************************************** Adventure Works / Adventure Works 2022 CSVs
https://github.com/microsoft/powerbi-desktop-samples/tree/main/DAX
https://www.kaggle.com/datasets/algorismus/adventure-works-in-excel-tables/data


PROFILE DATA
------------
Determining anomalies, examining and developing the underlying data structures, and querying data statistics such as row counts, value distributions, minimum and maximum values, averages.
By default, Power Query examines the first 1000 rows of your data set. To change this, select the profiling status in the status bar and select Column profiling based on entire data set.

Column distribution :  counts of distinct and unique values
Distinct values are all the different values in a column, including duplicates and null values, while unique values do not include duplicates or nulls. Therefore, distinct in this table tells
you the total count of how many values are present, while unique tells you how many of those values only appear once.

M Code
-------
in statement: This tells Power Query which step is the final output.
Top-down structure: Each step builds on the previous one.
Variable names: You define each step with a name on the left of the =.
Dependencies: Later steps rely on earlier ones—changing the order can break things.

Data model:
----------
A Power BI data model is a query-able data resource that’s optimized for analytics.A data model is also described as semantic model.
Power BI dataset:Once Power BI data model is published to a workspace in BI Service it is known as a dataset.
Analytic query:When Power BI visualizes dataset data, it prepares and sends an analytic query. An analytic query produces a query result from a model that’s easy for a person to understand, especially when visualized.
          An analytic query has three phases that are executed in this order:
          Filter
          Group
          Summarize
Tabular model:
Star schema design:
Table storage mode:
Model framework:
